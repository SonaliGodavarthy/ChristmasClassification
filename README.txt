For test data, ChristmasImages dataset implements dataset as tensor of tensors (tensor of transformed images).
For training data, ChristmasImages dataset implements dataset using ImageFolder.
For implementation of Transfer learning, GoogleNet is used as base model.
